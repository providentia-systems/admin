name: providentia_admin
description: Privacy-safe Linux desktop administration client for Providentia.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.12.2 <4.0.0"
  flutter: ">=3.44.7 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: 3.0.7
  flutter_secure_storage: 10.3.1
  http: 1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0

flutter:
  uses-material-design: true
